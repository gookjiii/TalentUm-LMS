name: Flutter CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  analyze:
    name: 🔍 Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Checkout external packages (path deps)
        run: |
          git clone --depth=1 https://github.com/flyerhq/flutter_chat_ui.git ../flutter_chat_ui
          git clone --depth=1 https://github.com/flyerhq/flutter_firebase_chat_core.git ../flutter_firebase_chat_core

      - name: Get dependencies
        run: flutter pub get

      - name: Generate localizations
        run: flutter gen-l10n

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Test
        run: flutter test
        continue-on-error: true
